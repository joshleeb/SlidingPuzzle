#include <iostream>
#include <memory>
#include <vector>

#include "state.h"

int main(int argc, char *argv[]) {
    std::vector<int> board {1, 2, 3, 4, 5, 6, 7, 8, 0};
    state target(board);

    std::cout << "Work in progress\n";
    return EXIT_SUCCESS;
}
